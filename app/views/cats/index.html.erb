<h1>99CATS</h1>

<table>
  <% @cats.each do |cat| %>
    <tr>
      <td>
        <%= link_to cat.name, cat_url(cat) %>: <%= cat.description %>
      </td>
    </tr>
  <% end %>
</table>
<br/>
<% if current_user %>
  <%= link_to "Register your cat", new_cat_url %>
<% end %>
